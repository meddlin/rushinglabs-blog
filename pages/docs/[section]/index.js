import Layout from '../../../components/layout';

const section = 'owasp'

function DocsSection() {
    return (
        <Layout>
            <h1>Docs: {section}</h1>
        </Layout>
    );
}

export default DocsSection;