import Layout from '../../components/layout';

function Docs() {
    return (
        <Layout>
            <h1>Docs is a space under active development.</h1>
        </Layout>
    );
}

export default Docs;